# This file remains for developers who prefer `pip install -r requirements.txt`.
# The canonical dependency definitions now live in `pyproject.toml`.

# Core runtime dependencies (installed automatically via `pip install fusesell`)
openai>=1.0.0
requests>=2.25.0
beautifulsoup4>=4.9.0
lxml>=4.6.0
python-dateutil>=2.8.0
pytz>=2021.1

# Optional: OCR support (`pip install fusesell[ocr]`)
pytesseract>=0.3.8
pillow>=8.0.0
easyocr>=1.6.0

# Optional: PDF tooling (`pip install fusesell[pdf]`)
PyPDF2>=3.0.0
PyMuPDF>=1.20.0

# Optional: Google Cloud Vision (`pip install fusesell[gcv]`)
google-cloud-vision>=2.0.0

# Optional: Development helpers (`pip install fusesell[dev]`)
psutil>=5.8.0
